# Tasks: UI Foundation (shadcn/ui + Radix)

## ルール

- 各タスクは spec の要求 ID に紐づける

## 実装

- [x] T-001 shadcn/ui + Radix 依存を追加する（FR-1）
  - 完了条件: 必要パッケージが `web/package.json` に追加される
- [x] T-002 UI基盤コンポーネントを追加する（FR-1）
  - 完了条件: Button/Card/Input/Select/Tabs/Separator/Skeleton/Tooltip/Toast が利用可能
- [x] T-003 トップページを1ページ構成に置換する（FR-2）
  - 完了条件: 主要操作へ直接到達できる1ページ構成になる
- [x] T-004 テーマ切替を実装する（FR-3）
  - 完了条件: ライト/ダーク切替が可能で見た目が崩れない
  - メモ: `ThemeProvider` / `ThemeToggle` は実装済み。トップページへのトグル配置は未反映
- [x] T-005 テーマ切替導線を画面に配置し、配色トークンをテーマ追従させる（FR-4）
  - 完了条件: ThemeToggle がUI上で操作可能、かつワークベンチ配色がライト/ダークで破綻しない
- [x] T-006 RGBキューブ/スライスにキーボードまたは代替UIでの色選択導線を追加する（FR-5）
  - 完了条件: ポインタを使わずに selectedColor を更新できる
- [x] T-007 内部要件ID表示を除去し、ユーザー向け情報設計へ置換する（FR-6）
  - 完了条件: `FR-*` 表示が画面上に存在せず、操作順が理解しやすい構成になる
- [x] T-008 可視化へ軸・凡例・説明文を追加し、結果解釈を補助する（FR-7）
  - 完了条件: 散布図/ヒストグラムに解釈補助UIが追加される
- [x] T-009 コピー/分析の通知を toast + aria-live で改善する（FR-8）
  - 完了条件: 成功/失敗/進行状態が視認可能かつ支援技術に通知される
- [x] T-010 ヘッダーをコンパクトで機能優先の見た目へ再設計する（FR-9）
  - 完了条件: ヘッダーがファーストビューを過度に占有しない

## テスト

- [x] T-101 lint と format:check を通過する（FR-1, FR-2, FR-3）
- [x] T-102 モバイル幅でレイアウト崩れがないことを確認する（NFR-2）
- [x] T-103 キーボード操作で Radix コンポーネントが操作可能であることを確認する（NFR-1）
- [x] T-104 ライト/ダーク切替時の可読性を確認する（FR-4, NFR-3）
- [x] T-105 ポインタ未使用で色選択が完了できることを確認する（FR-5）
- [x] T-106 可視化の軸/凡例/説明が表示されることを確認する（FR-7）
- [x] T-107 操作通知が aria-live で読み上げ可能であることを確認する（FR-8, NFR-4）
- [x] T-108 ヘッダーがモバイル/デスクトップ双方で過剰な高さを使わないことを確認する（FR-9）

## 監視 / 運用

- [x] T-201 今後追加する feature で `components/ui` 再利用を優先する運用ルールを共有する（FR-1）
