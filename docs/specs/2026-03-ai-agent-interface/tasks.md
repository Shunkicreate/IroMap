# Tasks: AI Agent対応画像解析インターフェース

## ルール

- 各タスクは `spec.md` の要求IDに紐づける
- 実装順は「制限 -> 共通解析モデル -> API -> HTML導線 -> 例外系 -> 観測」の順を優先する

## 実装

- [ ] T-001 IPベースのレート制限基盤を実装する（FR-7, FR-8）
  - 完了条件: IP単位制限を適用できる
- [ ] T-002 共通 `AnalysisResult` ドメインモデルを定義する（FR-2, FR-3, FR-5, FR-7）
  - 完了条件: HTML/JSONの両方で利用する入力・要約・解釈・可視化データ構造が定義されている
- [ ] T-003 画像入力アダプタを実装する（FR-1, FR-6, FR-7, FR-8）
  - 完了条件: file upload と raw image body を共通入力へ正規化できる
- [ ] T-004 サーバー側解析パイプラインを実装する（FR-1, FR-3, FR-5, FR-6, FR-8）
  - 完了条件: メタデータ、主要色、統計値、解釈文、可視化用データを生成できる
- [ ] T-005 `POST /api/analyze` を実装する（FR-1, FR-3, FR-7, FR-8）
  - 完了条件: raw body でJSONレスポンスと異常系レスポンスを返せる
- [ ] T-006 HTML Presenter を実装する（FR-2, FR-4, FR-5）
  - 完了条件: 見出し、サマリー、数値結果、図の説明を構造化HTMLとして出力できる
- [ ] T-007 `/analyze` 人間向け導線を実装する（FR-1, FR-2, FR-4, FR-5）
  - 完了条件: アップロード、プレビュー、結果表示を1画面で利用できる
- [ ] T-008 API利用ドキュメントを公開する（FR-7）
  - 完了条件: `/docs/agent-api` `openapi.json` `llms.txt` の少なくとも2つを提供する
- [ ] T-009 元画像非保存ポリシーに沿った一時処理を実装する（FR-6）
  - 完了条件: 永続ストレージへ元画像を保存しない構成で解析が完結する
- [ ] T-010 入力バリデーションとエラーモデルを実装する（FR-7, FR-8）
  - 完了条件: 非対応形式、サイズ超過、画素数超過、デコード失敗、レート制限超過を区別して返せる

## テスト

- [ ] T-101 raw image body でJSON解析結果を取得できることを確認する（FR-1, FR-3）
- [ ] T-102 ブラウザアップロードと raw body API で同一画像バイト列の結果が一致することを確認する（FR-1, FR-3）
- [ ] T-103 HTML結果にサマリー、数値結果、図の説明が含まれることを確認する（FR-2, FR-4, FR-5）
- [ ] T-104 HTMLとJSONで主要色や要約文が整合することを確認する（FR-2, FR-3, FR-5）
- [ ] T-105 元画像が永続保存されないことを確認する（FR-6）
- [ ] T-106 レート制限超過時に `429` を返すことを確認する（FR-8）
- [ ] T-107 `4.0 MB` 超過または `12 MP` 超過時に制限エラーを返すことを確認する（FR-8）

## 監視 / 運用

- [ ] T-201 解析処理時間と入力サイズを記録する（NFR-1, NFR-2）
- [ ] T-202 レート制限、サイズ超過を含む成功率 / 失敗率を記録する（NFR-1, NFR-4, NFR-5）
- [ ] T-203 limit 値を環境変数で調整できるようにする（NFR-5, NFR-6）
