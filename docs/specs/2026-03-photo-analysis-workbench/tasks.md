# Tasks: Photo Analysis Workbench

## ルール

- 各タスクは spec の要求 ID に紐づける
- MVP は P0 を先に閉じ、P1 は土台実装までを対象とする

## 実装

- [ ] T-001 ワークベンチ上段 3 ペインレイアウトを実装する（FR-1）
  - 完了条件: デスクトップ幅で `画像 / 3D / スライス` を同時表示でき、インスペクタは下段補助領域へ移る
- [ ] T-013 下段補助領域のスクロール設計を実装する（FR-1）
  - 完了条件: 上段 3 ペインを保ったまま下段情報を閲覧できる
- [ ] T-002 target / selection / hover を分離した workbench state を実装する（FR-2, FR-8）
  - 完了条件: source of truth と派生 state の境界がコード上で分かれている
- [ ] T-003 画像プレビューと 3D / スライスの相互ハイライト状態モデルを実装する（FR-2）
  - 完了条件: hover と selection が別状態で同一ビュー群へ共有される
- [ ] T-004 矩形選択と選択領域再集計を実装する（FR-3）
  - 完了条件: 選択領域に応じて指標表とヒストグラムが更新される
- [ ] T-014 selection clear と scope 復帰を実装する（FR-3）
  - 完了条件: clear 後に `full-image` へ戻る
- [ ] T-006 写真現像向け最小指標表を実装する（FR-5）
  - 完了条件: `highlight a* mean` を含む spec 記載の最小指標セットを表示できる
- [ ] T-019 指標ヘルプの `i` tooltip と `L* > 80` 条件明記を実装する（FR-5）
  - 完了条件: 値方向と意味が一致した説明を hover / focus で確認できる
- [ ] T-007 指標表コピーを `Markdown / CSV / TSV` で実装する（FR-6）
  - 完了条件: 既定値 `Markdown Table` を含む 3 形式でコピーできる
- [ ] T-008 `L* histogram` 表示と bin コピーを実装する（FR-6, FR-7）
  - 完了条件: `start / end / count / ratio` を出力できる
- [ ] T-020 RGBキューブ / スライスのマッピング表示切替を実装する（FR-2, FR-8）
  - 完了条件: 白マッピングと選択色マッピングを個別にオンオフできる
- [ ] T-021 折りたたみ UI と `localStorage` 永続化を共通コンポーネントで実装する（FR-8）
  - 完了条件: upload / options / inspector の開閉状態が次回訪問時も復元される
- [ ] T-022 画像アップロード UI を閉じても画面全体 paste が有効な導線へ変更する（FR-8）
  - 完了条件: 画像入力 disclosure を閉じても画像ペーストで解析を開始できる
- [ ] T-010 後続の履歴 / 選択保存に備えた snapshot state を導入する（FR-9）
  - 完了条件: 現在対象と選択状態を保存可能なデータ構造で保持できる
- [ ] T-011 指標の丸め、`N/A`、列順を共通 serializer に実装する（FR-5, FR-6）
  - 完了条件: UI 表示とコピー出力で同じ整形ルールを使う
- [ ] T-012 `L* histogram` の固定 bin 定義を実装する（FR-7）
  - 完了条件: `0 <= L* <= 100` の 20 bins 固定幅を維持する
- [ ] T-016 レスポンシブ時の 3 ペイン縦積みと状態保持を実装する（FR-1, NFR-4）
  - 完了条件: panel 順が変わっても selection state が維持される
- [ ] T-017 再集計トリガーと hover 非再計算ルールを実装する（NFR-1, NFR-5）
  - 完了条件: hover で重い再計算が発火しない
- [ ] T-018 テスト責務に沿って domain / component / e2e を分割する（NFR-1, NFR-2）
  - 完了条件: 主要仕様が層別に検証される

## テスト

- [ ] T-101 3 ペインレイアウトがデスクトップ幅で同時表示されることを確認する（FR-1）
- [ ] T-111 下段スクロール時も上段 3 ペインが利用可能であることを確認する（FR-1）
- [ ] T-102 hover が selection を破壊しないことを確認する（FR-2）
- [ ] T-103 画像上ホバーと 3D / スライスの相互ハイライトを確認する（FR-2）
- [ ] T-104 矩形選択後に局所再集計されることを確認する（FR-3）
- [ ] T-112 selection clear 後に full-image 集計へ戻ることを確認する（FR-3）
- [ ] T-116 `highlight a* mean` と `L* > 80` 条件表示を確認する（FR-5）
- [ ] T-106 指標表の 3 形式コピーを確認する（FR-6）
- [ ] T-107 `L* histogram` の表示と bin コピーを確認する（FR-6, FR-7）
- [ ] T-108 Highlight 条件なし、空選択、単色画像の各 edge case を確認する（FR-3, FR-5, FR-7）
- [ ] T-109 指標の丸めと `N/A` 表示が UI とコピーで一致することを確認する（FR-5, FR-6）
- [ ] T-117 マッピング表示切替が RGBキューブ / スライスで個別に効くことを確認する（FR-2, FR-8）
- [ ] T-118 折りたたみ状態がリロード後も復元されることを確認する（FR-8）
- [ ] T-119 upload disclosure を閉じても画面全体 paste が有効であることを確認する（FR-8）
- [ ] T-114 狭幅へ崩した後も selection state が保持されることを確認する（FR-1, NFR-4）
- [ ] T-115 hover 操作で再集計が走らないことを確認する（NFR-5）

## 監視 / 運用

- [ ] T-201 再集計時間を記録できるようにする（NFR-1）
- [ ] T-202 同一画像、同一選択条件で結果再現性を確認する（NFR-2）
- [ ] T-203 コピー操作の成功通知と失敗通知を確認する（FR-6, NFR-4）
- [ ] T-204 hover 応答と selection 再集計のログを区別して確認できるようにする（NFR-5）
