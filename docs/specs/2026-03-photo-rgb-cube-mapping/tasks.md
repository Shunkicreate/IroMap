# Tasks: Photo RGB Cube Mapping

## 実装

- [ ] T-001 画像サンプルから RGB キューブ点群を生成する（FR-1, FR-4, FR-5）
  - 完了条件: `PhotoAnalysisResult` に量子化済み `cubePoints` が含まれる
- [ ] T-002 Photo Analysis から Workbench へ画像点群を連携する（FR-3）
  - 完了条件: 画像選択・再選択・クリア時に workbench 状態が同期する
- [ ] T-003 RGB キューブに画像点群 overlay 描画を追加する（FR-1, FR-5）
  - 完了条件: 画像由来点群が固定グリッドとは独立して描画される
- [ ] T-004 RGB キューブの表示モード切り替え UI を追加する（FR-2）
  - 完了条件: `grid only` / `image only` / `grid + image` を選択できる
- [ ] T-005 画像点群表示時の描画点数・見た目パラメータを調整する（FR-4, FR-5）
  - 完了条件: 上限点数、alpha、必要に応じた点サイズの初期値が定義される

## テスト

- [ ] T-101 画像アップロード後に RGB キューブへ点群表示されることを確認する（FR-1）
- [ ] T-102 表示モード切り替えで描画内容が変わることを確認する（FR-2）
- [ ] T-103 画像変更・クリア時に点群が同期して更新されることを確認する（FR-3）
- [ ] T-104 大きな画像でも点群数が上限制御されることを確認する（FR-4）
- [ ] T-105 頻出色が低頻度色より視認しやすく描画されることを確認する（FR-5）

## 監視 / 運用

- [ ] T-201 入力画像サイズ、`sampledPixels`、`cubePoints.length` を用いて性能確認を行う（NFR-1, NFR-2）
- [ ] T-202 同一画像の再分析で `cubePoints` が再現されることを確認する（NFR-2）
