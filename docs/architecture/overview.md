# Architecture Overview

このディレクトリでは、IroMap のシステム全体構造を定義します。

- `context.md`: システム境界と外部要素
- `containers.md`: 実行単位（アプリ、API、DB など）
- `components.md`: コンテナ内の主要コンポーネント
- `data-model.md`: ドメインとデータ構造
- `quality-attributes.md`: 品質特性と設計方針
- `diagrams/`: 補助図（Mermaid, 画像など）
