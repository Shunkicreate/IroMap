interface:
  display_name: "Git Worktree"
  short_description: "Create and manage isolated git worktrees"
  default_prompt: "Use $git-worktree to 作業用 worktree を作成または再利用し、web の pnpm install と hooks 設定まで実行してください。必要なら削除まで実行してください。"
